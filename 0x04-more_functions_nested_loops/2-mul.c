#include "main.h"

/**
 * mul - a function that multiplies two integers
 *
 * @a: first integer
 * @b: second integer
 *
 * Return: 0 (Success)
 */
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
